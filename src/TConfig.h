#pragma once

namespace transmitter {
  const char* DEFAULT_MASTER_SERVER = "https://zeug.tobias-kozel.de";
  const int MAX_BUFFER_SIZE = 512;
  const char* STR_OWNID_HINT = "Connect first to receive an ID.";
}